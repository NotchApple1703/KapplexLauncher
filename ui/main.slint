import { SideBar } from "@ui/components/sidebar/sidebar.slint";

import { IndicatorPosition } from "@ui/enums.slint";
import { Style } from "@ui/globals.slint";

export * from "@ui/globals.slint";

export component MainWindow inherits Window {
    in-out property <int> active-page: 0;

    title: "LK Launcher";
    min-height: 500px;
    max-height: 10000px;

    background: Style.palette.background;

    HorizontalLayout {
        // Main View
        Rectangle {
            min-width: 600px;
            background: Style.palette.background;
        }

        SideBar {
            width: 220px;
            item-height: 60px;
            item-font-size: 22px;
            item-indicator_position: IndicatorPosition.LEFT;
            background: Style.palette.alternate-background;

            activate(index) => {
                root.active-page = index;
            }
        } 
    }
}